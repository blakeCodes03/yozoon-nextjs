// src/pages/coin/[id].tsx

import React from 'react';

// Minimal stub for legacy page to avoid server-side Prisma during build
export default function LegacyCoinPage() {
  return (
    <div>
      <h1>Legacy coin page (disabled)</h1>
      <p>This legacy page has been disabled for build stability.</p>
    </div>
  );
}
