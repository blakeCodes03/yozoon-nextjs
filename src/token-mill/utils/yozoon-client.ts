// Minimal shim for YozoonClient used by legacy contexts/components.
// The real implementation lives elsewhere; this lightweight stub keeps the
// frontend build happy for feature work.
export class YozoonClient {
  // Add any minimal methods callers expect. For now keep it empty.
}

export default YozoonClient;
